#   Wrote a program to calculate the speed.

distance = float(input("Enter the Distance Where you want to go: "))
time = float(input("How Much Time You Want to reach: "))

# speed = distance / time

print(f"The Speed You Must Have to Maintain, That is: {distance/time}")